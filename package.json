{
  "name": "web2md",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile ./src/index.ts --outfile web2md --external electron --external chromium-bidi",
    "test": "bun test",
    "clean": "rm -f *.bun-build"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "commander": "^11.0.0",
    "turndown": "^7.1.2",
    "turndown-plugin-gfm": "^1.0.2",
    "chalk": "^5.3.0",
    "ora": "^7.0.1",
    "p-limit": "^5.0.0"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.4",
    "bun-types": "^1.0.0"
  }
}